// Define the API endpoint and message parameters
const url = 'https://platform.clickatell.com/v1/message';
const apiKey = 'XhyyYtGkTDK--7vQExzdkw==';
const phone = '++447385643880'; // Replace with the user's phone number
const message = `Your password reset code is: ${otp}`; // Replace with the OTP generated by your system

// Send the SMS message
const xhr = new XMLHttpRequest();
xhr.open('POST', url, true);
xhr.setRequestHeader('Content-Type', 'application/json');
xhr.setRequestHeader('Authorization', `Bearer ${apiKey}`);
xhr.onreadystatechange = function() {
  if (xhr.readyState === XMLHttpRequest.DONE) {
    if (xhr.status === 202) {
      // Message sent successfully
      console.log('Message sent');
    } else {
      // Error sending message
      console.error('Error sending message:', xhr.responseText);
    }
  }
};
xhr.send(JSON.stringify({
  messages: [{ to: phone, content: message }],
}));
